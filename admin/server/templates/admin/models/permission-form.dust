{>"admin/layouts/form" model="Permission" /}
{<form}
<form class="add permission">
	<div class="server error"></div>
	<div>
		<label>Regex</label>
		<input type="text" data-hook="regex" name="regex" />
	</div>
	<div>
		{#roles}
			<input type="checkbox" data-hook="roles" name="roles" value="{name}" />{name}
		{/roles}
	</div>
</form>
{/form}
